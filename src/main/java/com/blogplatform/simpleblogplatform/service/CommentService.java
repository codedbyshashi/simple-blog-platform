package com.blogplatform.simpleblogplatform.service;

import org.springframework.stereotype.Service;

/**
 * The CommentService class encapsulates the business logic for all operations
 * related to comments. It acts as an intermediary between the controllers
 * and the CommentRepository.
 */
@Service // Marks this class as a Spring service bean, making it a candidate for dependency injection.
public class CommentService {

    // In the next task, we will inject the CommentRepository here to give this
    // service the ability to interact with the database. For now, this class
    // serves as the foundational component of our comment-related business logic.

}